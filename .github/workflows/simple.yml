name: Simple Concept Workflow

on:
    push:
    workflow_dispatch:

jobs:
    learn:
        runs-on: ubuntu-latest
        steps:
            - name: Hello world
              run: echo "Hello World" 